name: 🚀 Build and Deploy Fineract WAR

on:
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: ☕ Set up Java 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: 🔧 Make Gradle Executable
        run: chmod +x ./gradlew

      - name: 🛠 Build Proper Fineract WAR
        run: ./gradlew :fineract-war:war -x test -x asciidoctor -x asciidoctorPdf -x spotbugsMain -x spotbugsTest

      - name: 📦 Locate WAR File
        id: warfile
        run: |
          FILE=$(ls fineract-war/build/libs/fineract-war*.war | head -n 1)
          echo "WAR_PATH=$FILE" >> $GITHUB_ENV
          echo "✅ Found WAR at: $FILE"

      - name: 🚀 Upload WAR to Tomcat
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: ${{ env.WAR_PATH }}
          target: /var/lib/tomcat9/webapps/fineract-provider.war

      - name: 🔁 Restart Tomcat
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo systemctl restart tomcat9
            echo "✅ Tomcat restarted"
